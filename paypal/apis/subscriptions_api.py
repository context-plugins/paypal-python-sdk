"""paypal.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: D410, E501, E101, D206
from apimatic_core.authentication.multiple.single_auth import (
    Single,
)
from apimatic_core.request_builder import RequestBuilder
from apimatic_core.response_handler import ResponseHandler
from apimatic_core.types.parameter import Parameter

from paypal.api_helper import APIHelper
from paypal.apis.base_api import BaseApi
from paypal.configuration import Server
from paypal.exceptions.subscription_error_exception import (
    SubscriptionErrorException,
)
from paypal.http.http_method_enum import HttpMethodEnum
from paypal.models.billing_plan import BillingPlan
from paypal.models.modify_subscription_response import (
    ModifySubscriptionResponse,
)
from paypal.models.plan_collection import PlanCollection
from paypal.models.subscription import Subscription
from paypal.models.subscription_collection import (
    SubscriptionCollection,
)
from paypal.models.subscription_transaction_details import (
    SubscriptionTransactionDetails,
)
from paypal.models.transactions_list import (
    TransactionsList,
)


class SubscriptionsApi(BaseApi):
    """A Controller to access Endpoints in the paypal API."""

    def __init__(self, config):
        """Initialize SubscriptionsApi object."""
        super(SubscriptionsApi, self).__init__(config)

    def create_billing_plan(self,
                            prefer="return=minimal",
                            pay_pal_request_id=None,
                            body=None):
        """Perform a POST request to /v1/billing/plans.

        Creates a plan that defines pricing and billing cycle details for
        subscriptions.

        Args:
            prefer (str, optional): The preferred server response upon successful
                completion of the request. Value is: return=minimal. The server
                returns a minimal response to optimize communication between the API
                caller and the server. A minimal response includes the id, status and
                HATEOAS links. return=representation. The server returns a complete
                resource representation, including the current state of the resource.
            pay_pal_request_id (str, optional): The server stores keys for 72 hours.
            body (PlanRequest, optional): The request body parameter.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. A successful request
                returns the HTTP `200 OK` status code and a JSON response body that
                shows billing plan details.

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/billing/plans")
            .http_method(HttpMethodEnum.POST)
            .header_param(Parameter()
                .key("Content-Type")
                .value("application/json"))
            .header_param(Parameter()
                .key("Prefer")
                .value(prefer))
            .header_param(Parameter()
                .key("PayPal-Request-Id")
                .value(pay_pal_request_id))
            .body_param(Parameter()
                .value(body))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .body_serializer(APIHelper.json_serialize)
            .auth(Single("Oauth2")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(BillingPlan.from_dictionary)
            .is_api_response(True)
            .local_error("400",
                "Bad Request. Request is not well-formed, syntactically incorrect, or"
                " violates schema.",
                SubscriptionErrorException)
            .local_error("401",
                "Authentication failed due to missing authorization header, or invali"
                "d authentication credentials.",
                SubscriptionErrorException)
            .local_error("403",
                "Authorization failed due to insufficient permissions.",
                SubscriptionErrorException)
            .local_error("422",
                "The requested action could not be performed, semantically incorrect,"
                " or failed business validation.",
                SubscriptionErrorException)
            .local_error("500",
                "An internal server error has occurred.",
                SubscriptionErrorException)
            .local_error("default", "The error response.", SubscriptionErrorException),
        ).execute()

    def list_billing_plans(self,
                           prefer="return=minimal",
                           product_id=None,
                           page_size=10,
                           page=1,
                           total_required=False):
        """Perform a GET request to /v1/billing/plans.

        Lists billing plans.

        Args:
            prefer (str, optional): The preferred server response upon successful
                completion of the request. Value is: return=minimal. The server
                returns a minimal response to optimize communication between the API
                caller and the server. A minimal response includes the id, name,
                description and HATEOAS links. return=representation. The server
                returns a complete resource representation, including the current
                state of the resource.
            product_id (str, optional): Filters the response by a Product ID.
            page_size (int, optional): The number of items to return in the response.
            page (int, optional): A non-zero integer which is the start index of the
                entire list of items to return in the response. The combination of
                `page=1` and `page_size=20` returns the first 20 items. The
                combination of `page=2` and `page_size=20` returns the next 20 items.
            total_required (bool, optional): Indicates whether to show the total
                count in the response.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. A successful request
                returns the HTTP `200 OK` status code and a JSON response body that
                lists billing plans.

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/billing/plans")
            .http_method(HttpMethodEnum.GET)
            .header_param(Parameter()
                .key("Prefer")
                .value(prefer))
            .query_param(Parameter()
                .key("product_id")
                .value(product_id))
            .query_param(Parameter()
                .key("page_size")
                .value(page_size))
            .query_param(Parameter()
                .key("page")
                .value(page))
            .query_param(Parameter()
                .key("total_required")
                .value(total_required))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("Oauth2")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(PlanCollection.from_dictionary)
            .is_api_response(True)
            .local_error("400",
                "Request is not well-formed, syntactically incorrect, or violates sch"
                "ema.",
                SubscriptionErrorException)
            .local_error("401",
                "Authentication failed due to missing authorization header, or invali"
                "d authentication credentials.",
                SubscriptionErrorException)
            .local_error("403",
                "Authorization failed due to insufficient permissions.",
                SubscriptionErrorException)
            .local_error("404",
                "The specified resource does not exist.",
                SubscriptionErrorException)
            .local_error("500",
                "An internal server error has occurred.",
                SubscriptionErrorException)
            .local_error("default", "The error response.", SubscriptionErrorException),
        ).execute()

    def get_billing_plan(self,
                         id):
        """Perform a GET request to /v1/billing/plans/{id}.

        Shows details for a plan, by ID.

        Args:
            id (str): The ID of the plan.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. A successful request
                returns the HTTP `200 OK` status code and a JSON response body that
                shows plan details.

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/billing/plans/{id}")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("id")
                .value(id)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("Oauth2")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(BillingPlan.from_dictionary)
            .is_api_response(True)
            .local_error("401",
                "Authentication failed due to missing authorization header, or invali"
                "d authentication credentials.",
                SubscriptionErrorException)
            .local_error("403",
                "Authorization failed due to insufficient permissions.",
                SubscriptionErrorException)
            .local_error("404",
                "The specified resource does not exist.",
                SubscriptionErrorException)
            .local_error("500",
                "An internal server error has occurred.",
                SubscriptionErrorException)
            .local_error("default", "The error response.", SubscriptionErrorException),
        ).execute()

    def patch_billing_plan(self,
                           id,
                           body=None):
        """Perform a PATCH request to /v1/billing/plans/{id}.

        Updates a plan with the `CREATED` or `ACTIVE` status. For an `INACTIVE` plan,
        you can make only status updates. You can patch these attributes and objects:
        Attribute or object Operations description replace
        payment_preferences.auto_bill_outstanding replace taxes.percentage replace
        payment_preferences.payment_failure_threshold replace
        payment_preferences.setup_fee replace
        payment_preferences.setup_fee_failure_action replace name replace

        Args:
            id (str): The ID of the plan.
            body (List[Patch], optional): The request body parameter.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. A successful request
                returns the HTTP `204 No Content` status code with no JSON response
                body.

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/billing/plans/{id}")
            .http_method(HttpMethodEnum.PATCH)
            .template_param(Parameter()
                .key("id")
                .value(id)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("Content-Type")
                .value("application/json"))
            .body_param(Parameter()
                .value(body))
            .body_serializer(APIHelper.json_serialize)
            .auth(Single("Oauth2")),
        ).response(
            ResponseHandler()
            .is_api_response(True)
            .local_error("400",
                "Request is not well-formed, syntactically incorrect, or violates sch"
                "ema.",
                SubscriptionErrorException)
            .local_error("401",
                "Authentication failed due to missing authorization header, or invali"
                "d authentication credentials.",
                SubscriptionErrorException)
            .local_error("403",
                "Authorization failed due to insufficient permissions.",
                SubscriptionErrorException)
            .local_error("404",
                "The specified resource does not exist.",
                SubscriptionErrorException)
            .local_error("422",
                "The requested action could not be performed, semantically incorrect,"
                " or failed business validation.",
                SubscriptionErrorException)
            .local_error("500",
                "An internal server error has occurred.",
                SubscriptionErrorException)
            .local_error("default", "The error response.", SubscriptionErrorException),
        ).execute()

    def activate_billing_plan(self,
                              id):
        """Perform a POST request to /v1/billing/plans/{id}/activate.

        Activates a plan, by ID.

        Args:
            id (str): The ID of the plan.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. A successful request
                returns the HTTP `204 No Content` status code with no JSON response
                body.

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/billing/plans/{id}/activate")
            .http_method(HttpMethodEnum.POST)
            .template_param(Parameter()
                .key("id")
                .value(id)
                .is_required(True)
                .should_encode(True))
            .auth(Single("Oauth2")),
        ).response(
            ResponseHandler()
            .is_api_response(True)
            .local_error("401",
                "Authentication failed due to missing authorization header, or invali"
                "d authentication credentials.",
                SubscriptionErrorException)
            .local_error("403",
                "Authorization failed due to insufficient permissions.",
                SubscriptionErrorException)
            .local_error("404",
                "The specified resource does not exist.",
                SubscriptionErrorException)
            .local_error("422",
                "The requested action could not be performed, semantically incorrect,"
                " or failed business validation.",
                SubscriptionErrorException)
            .local_error("500",
                "An internal server error has occurred.",
                SubscriptionErrorException)
            .local_error("default", "The error response.", SubscriptionErrorException),
        ).execute()

    def deactivate_billing_plan(self,
                                id):
        """Perform a POST request to /v1/billing/plans/{id}/deactivate.

        Deactivates a plan, by ID.

        Args:
            id (str): The ID of the plan.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. A successful request
                returns the HTTP `204 No Content` status code with no JSON response
                body.

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/billing/plans/{id}/deactivate")
            .http_method(HttpMethodEnum.POST)
            .template_param(Parameter()
                .key("id")
                .value(id)
                .is_required(True)
                .should_encode(True))
            .auth(Single("Oauth2")),
        ).response(
            ResponseHandler()
            .is_api_response(True)
            .local_error("401",
                "Authentication failed due to missing authorization header, or invali"
                "d authentication credentials.",
                SubscriptionErrorException)
            .local_error("403",
                "Authorization failed due to insufficient permissions.",
                SubscriptionErrorException)
            .local_error("404",
                "The specified resource does not exist.",
                SubscriptionErrorException)
            .local_error("422",
                "The requested action could not be performed, semantically incorrect,"
                " or failed business validation.",
                SubscriptionErrorException)
            .local_error("500",
                "An internal server error has occurred.",
                SubscriptionErrorException)
            .local_error("default", "The error response.", SubscriptionErrorException),
        ).execute()

    def update_billing_plan_pricing_schemes(self,
                                            id,
                                            body=None):
        """Perform a POST request to
        /v1/billing/plans/{id}/update-pricing-schemes.

        Updates pricing for a plan. For example, you can update a regular billing
        cycle from $5 per month to $7 per month.

        Args:
            id (str): The ID for the plan.
            body (UpdatePricingSchemesRequest, optional): The request body parameter.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. A successful request
                returns the HTTP `204 No Content` status code with no JSON response
                body.

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/billing/plans/{id}/update-pricing-schemes")
            .http_method(HttpMethodEnum.POST)
            .template_param(Parameter()
                .key("id")
                .value(id)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("Content-Type")
                .value("application/json"))
            .body_param(Parameter()
                .value(body))
            .body_serializer(APIHelper.json_serialize)
            .auth(Single("Oauth2")),
        ).response(
            ResponseHandler()
            .is_api_response(True)
            .local_error("400",
                "Bad Request. Request is not well-formed, syntactically incorrect, or"
                " violates schema.",
                SubscriptionErrorException)
            .local_error("401",
                "Authentication failed due to missing authorization header, or invali"
                "d authentication credentials.",
                SubscriptionErrorException)
            .local_error("403",
                "Authorization failed due to insufficient permissions.",
                SubscriptionErrorException)
            .local_error("404",
                "The specified resource does not exist.",
                SubscriptionErrorException)
            .local_error("422",
                "The requested action could not be performed, semantically incorrect,"
                " or failed business validation.",
                SubscriptionErrorException)
            .local_error("500",
                "An internal server error has occurred.",
                SubscriptionErrorException)
            .local_error("default", "The error response.", SubscriptionErrorException),
        ).execute()

    def create_subscription(self,
                            prefer="return=minimal",
                            pay_pal_request_id=None,
                            pay_pal_client_metadata_id=None,
                            body=None):
        """Perform a POST request to /v1/billing/subscriptions.

        Creates a subscription.

        Args:
            prefer (str, optional): The preferred server response upon successful
                completion of the request. Value is: return=minimal. The server
                returns a minimal response to optimize communication between the API
                caller and the server. A minimal response includes the id, status and
                HATEOAS links. return=representation. The server returns a complete
                resource representation, including the current state of the resource.
            pay_pal_request_id (str, optional): The server stores keys for 72 hours.
            pay_pal_client_metadata_id (str, optional): The PayPal Client Metadata
                Id(CMID) is used to provide device-specific information to PayPal's
                risk engine. This is crucial for transactions that require
                device-specific risk assessments. Merchants typically use the Paypal
                SDK that automatically submits the CMID or they use tools like
                Fraudnet JS for web or Magnes JS for mobile to generate the CMID on
                the frontend and then pass it to the API as part of the request
                headers.
            body (CreateSubscriptionRequest, optional): The request body parameter.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. A successful request
                returns the HTTP `200 OK` status code and a JSON response body that
                shows subscription details.

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/billing/subscriptions")
            .http_method(HttpMethodEnum.POST)
            .header_param(Parameter()
                .key("Content-Type")
                .value("application/json"))
            .header_param(Parameter()
                .key("Prefer")
                .value(prefer))
            .header_param(Parameter()
                .key("PayPal-Request-Id")
                .value(pay_pal_request_id))
            .header_param(Parameter()
                .key("PayPal-Client-Metadata-Id")
                .value(pay_pal_client_metadata_id))
            .body_param(Parameter()
                .value(body))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .body_serializer(APIHelper.json_serialize)
            .auth(Single("Oauth2")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(Subscription.from_dictionary)
            .is_api_response(True)
            .local_error("400",
                "Bad Request. Request is not well-formed, syntactically incorrect, or"
                " violates schema.",
                SubscriptionErrorException)
            .local_error("401",
                "Authentication failed due to missing authorization header, or invali"
                "d authentication credentials.",
                SubscriptionErrorException)
            .local_error("403",
                "Authorization failed due to insufficient permissions.",
                SubscriptionErrorException)
            .local_error("422",
                "The requested action could not be performed, semantically incorrect,"
                " or failed business validation.",
                SubscriptionErrorException)
            .local_error("500",
                "An internal server error has occurred.",
                SubscriptionErrorException)
            .local_error("default", "The error response.", SubscriptionErrorException),
        ).execute()

    def list_subscriptions(self,
                           plan_ids=None,
                           statuses=None,
                           created_after=None,
                           created_before=None,
                           status_updated_before=None,
                           status_updated_after=None,
                           filter=None,
                           page_size=10,
                           page=1,
                           customer_ids=None):
        """Perform a GET request to /v1/billing/subscriptions.

        List all subscriptions for merchant account.

        Args:
            plan_ids (str, optional): Filters the response by list of plan IDs.
                Filter supports upto 70 plan IDs. URLs should not exceed a length of
                2000 characters.
            statuses (str, optional): Filters the response by list of subscription
                statuses.
            created_after (str, optional): Filters the response by subscription
                creation start time for a range of subscriptions.
            created_before (str, optional): Filters the response by subscription
                creation end time for a range of subscriptions.
            status_updated_before (str, optional): Filters the response by status
                update start time for a range of subscriptions.
            status_updated_after (str, optional): Filters the response by status
                update end time for a range of subscriptions.
            filter (str, optional): Filter the response using complex expressions
                that could use comparison operators like ge, gt, le, lt and logical
                operators such as 'and' and 'or'.
            page_size (int, optional): The number of items to return in the response.
            page (int, optional): A non-zero integer which is the start index of the
                entire list of items to return in the response. The combination of
                `page=1` and `page_size=20` returns the first 20 items. The
                combination of `page=2` and `page_size=20` returns the next 20 items.
            customer_ids (List[str], optional): Filters the response by comma
                separated vault customer IDs (FSS subscriptions only).

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. A successful request
                returns the HTTP `200 OK` status code and a JSON response body that
                lists the subscriptions.

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/billing/subscriptions")
            .http_method(HttpMethodEnum.GET)
            .query_param(Parameter()
                .key("plan_ids")
                .value(plan_ids))
            .query_param(Parameter()
                .key("statuses")
                .value(statuses))
            .query_param(Parameter()
                .key("created_after")
                .value(created_after))
            .query_param(Parameter()
                .key("created_before")
                .value(created_before))
            .query_param(Parameter()
                .key("status_updated_before")
                .value(status_updated_before))
            .query_param(Parameter()
                .key("status_updated_after")
                .value(status_updated_after))
            .query_param(Parameter()
                .key("filter")
                .value(filter))
            .query_param(Parameter()
                .key("page_size")
                .value(page_size))
            .query_param(Parameter()
                .key("page")
                .value(page))
            .query_param(Parameter()
                .key("customer_ids")
                .value(customer_ids))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("Oauth2")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(SubscriptionCollection.from_dictionary)
            .is_api_response(True)
            .local_error("400",
                "Request is not well-formed, syntactically incorrect, or violates sch"
                "ema.",
                SubscriptionErrorException)
            .local_error("401",
                "Authentication failed due to missing authorization header, or invali"
                "d authentication credentials.",
                SubscriptionErrorException)
            .local_error("403",
                "Authorization failed due to insufficient permissions.",
                SubscriptionErrorException)
            .local_error("500",
                "An internal server error has occurred.",
                SubscriptionErrorException)
            .local_error("default", "The error response.", SubscriptionErrorException),
        ).execute()

    def get_subscription(self,
                         id,
                         fields=None):
        """Perform a GET request to /v1/billing/subscriptions/{id}.

        Shows details for a subscription, by ID.

        Args:
            id (str): The ID of the subscription.
            fields (str, optional): List of fields that are to be returned in the
                response. Possible value for fields are last_failed_payment and plan.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. A successful request
                returns the HTTP `200 OK` status code and a JSON response body that
                shows subscription details.

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/billing/subscriptions/{id}")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("id")
                .value(id)
                .is_required(True)
                .should_encode(True))
            .query_param(Parameter()
                .key("fields")
                .value(fields))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("Oauth2")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(Subscription.from_dictionary)
            .is_api_response(True)
            .local_error("401",
                "Authentication failed due to missing authorization header, or invali"
                "d authentication credentials.",
                SubscriptionErrorException)
            .local_error("403",
                "Authorization failed due to insufficient permissions.",
                SubscriptionErrorException)
            .local_error("404",
                "The specified resource does not exist.",
                SubscriptionErrorException)
            .local_error("500",
                "An internal server error has occurred.",
                SubscriptionErrorException)
            .local_error("default", "The error response.", SubscriptionErrorException),
        ).execute()

    def patch_subscription(self,
                           id,
                           body=None):
        """Perform a PATCH request to /v1/billing/subscriptions/{id}.

        Updates a subscription which could be in ACTIVE or SUSPENDED status. You can
        override plan level default attributes by providing customised values for
        plan path in the patch request. You cannot update attributes that have
        already completed (Example - trial cycles can’t be updated if completed).
        Once overridden, changes to plan resource will not impact subscription. Any
        price update will not impact billing cycles within next 10 days (Applicable
        only for subscriptions funded by PayPal account). Following are the fields
        eligible for patch. Attribute or object Operations
        billing_info.outstanding_balance replace custom_id add,replace
        plan.billing_cycles[@sequence==n]. pricing_scheme.fixed_price add,replace
        plan.billing_cycles[@sequence==n]. pricing_scheme.tiers replace
        plan.billing_cycles[@sequence==n]. total_cycles replace
        plan.payment_preferences. auto_bill_outstanding replace
        plan.payment_preferences. payment_failure_threshold replace
        plan.taxes.inclusive add,replace plan.taxes.percentage add,replace
        shipping_amount add,replace start_time replace subscriber.shipping_address
        add,replace subscriber.payment_source (for subscriptions funded by card
        payments) replace

        Args:
            id (str): The ID for the subscription.
            body (List[Patch], optional): The request body parameter.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. A successful request
                returns the HTTP `204 No Content` status code with no JSON response
                body.

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/billing/subscriptions/{id}")
            .http_method(HttpMethodEnum.PATCH)
            .template_param(Parameter()
                .key("id")
                .value(id)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("Content-Type")
                .value("application/json"))
            .body_param(Parameter()
                .value(body))
            .body_serializer(APIHelper.json_serialize)
            .auth(Single("Oauth2")),
        ).response(
            ResponseHandler()
            .is_api_response(True)
            .local_error("400",
                "Request is not well-formed, syntactically incorrect, or violates sch"
                "ema.",
                SubscriptionErrorException)
            .local_error("401",
                "Authentication failed due to missing authorization header, or invali"
                "d authentication credentials.",
                SubscriptionErrorException)
            .local_error("403",
                "Authorization failed due to insufficient permissions.",
                SubscriptionErrorException)
            .local_error("404",
                "The specified resource does not exist.",
                SubscriptionErrorException)
            .local_error("422",
                "The requested action could not be performed, semantically incorrect,"
                " or failed business validation.",
                SubscriptionErrorException)
            .local_error("500",
                "An internal server error has occurred.",
                SubscriptionErrorException)
            .local_error("default", "The error response.", SubscriptionErrorException),
        ).execute()

    def revise_subscription(self,
                            id,
                            body=None):
        """Perform a POST request to /v1/billing/subscriptions/{id}/revise.

        Updates the quantity of the product or service in a subscription. You can
        also use this method to switch the plan and update the `shipping_amount`,
        `shipping_address` values for the subscription. This type of update requires
        the buyer's consent.

        Args:
            id (str): The ID of the subscription.
            body (ModifySubscriptionRequest, optional): The request body parameter.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. A successful request
                returns the HTTP `200 OK` status code and a JSON response body that
                shows subscription details.

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/billing/subscriptions/{id}/revise")
            .http_method(HttpMethodEnum.POST)
            .template_param(Parameter()
                .key("id")
                .value(id)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("Content-Type")
                .value("application/json"))
            .body_param(Parameter()
                .value(body))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .body_serializer(APIHelper.json_serialize)
            .auth(Single("Oauth2")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ModifySubscriptionResponse.from_dictionary)
            .is_api_response(True)
            .local_error("400",
                "Bad Request. Request is not well-formed, syntactically incorrect, or"
                " violates schema.",
                SubscriptionErrorException)
            .local_error("401",
                "Authentication failed due to missing authorization header, or invali"
                "d authentication credentials.",
                SubscriptionErrorException)
            .local_error("403",
                "Authorization failed due to insufficient permissions.",
                SubscriptionErrorException)
            .local_error("404",
                "The specified resource does not exist.",
                SubscriptionErrorException)
            .local_error("422",
                "The requested action could not be performed, semantically incorrect,"
                " or failed business validation.",
                SubscriptionErrorException)
            .local_error("500",
                "An internal server error has occurred.",
                SubscriptionErrorException)
            .local_error("default", "The error response.", SubscriptionErrorException),
        ).execute()

    def suspend_subscription(self,
                             id,
                             body=None):
        """Perform a POST request to /v1/billing/subscriptions/{id}/suspend.

        Suspends the subscription.

        Args:
            id (str): The ID of the subscription.
            body (SuspendSubscription, optional): The request body parameter.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. A successful request
                returns the HTTP `204 No Content` status code with no JSON response
                body.

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/billing/subscriptions/{id}/suspend")
            .http_method(HttpMethodEnum.POST)
            .template_param(Parameter()
                .key("id")
                .value(id)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("Content-Type")
                .value("application/json"))
            .body_param(Parameter()
                .value(body))
            .body_serializer(APIHelper.json_serialize)
            .auth(Single("Oauth2")),
        ).response(
            ResponseHandler()
            .is_api_response(True)
            .local_error("400",
                "Bad Request. Request is not well-formed, syntactically incorrect, or"
                " violates schema.",
                SubscriptionErrorException)
            .local_error("401",
                "Authentication failed due to missing authorization header, or invali"
                "d authentication credentials.",
                SubscriptionErrorException)
            .local_error("403",
                "Authorization failed due to insufficient permissions.",
                SubscriptionErrorException)
            .local_error("404",
                "The specified resource does not exist.",
                SubscriptionErrorException)
            .local_error("422",
                "The requested action could not be performed, semantically incorrect,"
                " or failed business validation.",
                SubscriptionErrorException)
            .local_error("500",
                "An internal server error has occurred.",
                SubscriptionErrorException)
            .local_error("default", "The error response.", SubscriptionErrorException),
        ).execute()

    def cancel_subscription(self,
                            id,
                            body=None):
        """Perform a POST request to /v1/billing/subscriptions/{id}/cancel.

        Cancels the subscription.

        Args:
            id (str): The ID of the subscription.
            body (CancelSubscriptionRequest, optional): The request body parameter.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. A successful request
                returns the HTTP `204 No Content` status code with no JSON response
                body.

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/billing/subscriptions/{id}/cancel")
            .http_method(HttpMethodEnum.POST)
            .template_param(Parameter()
                .key("id")
                .value(id)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("Content-Type")
                .value("application/json"))
            .body_param(Parameter()
                .value(body))
            .body_serializer(APIHelper.json_serialize)
            .auth(Single("Oauth2")),
        ).response(
            ResponseHandler()
            .is_api_response(True)
            .local_error("400",
                "Bad Request. Request is not well-formed, syntactically incorrect, or"
                " violates schema.",
                SubscriptionErrorException)
            .local_error("401",
                "Authentication failed due to missing authorization header, or invali"
                "d authentication credentials.",
                SubscriptionErrorException)
            .local_error("403",
                "Authorization failed due to insufficient permissions.",
                SubscriptionErrorException)
            .local_error("404",
                "The specified resource does not exist.",
                SubscriptionErrorException)
            .local_error("422",
                "The requested action could not be performed, semantically incorrect,"
                " or failed business validation.",
                SubscriptionErrorException)
            .local_error("500",
                "An internal server error has occurred.",
                SubscriptionErrorException)
            .local_error("default", "The error response.", SubscriptionErrorException),
        ).execute()

    def activate_subscription(self,
                              id,
                              body=None):
        """Perform a POST request to /v1/billing/subscriptions/{id}/activate.

        Activates the subscription.

        Args:
            id (str): The ID of the subscription.
            body (ActivateSubscriptionRequest, optional): The request body parameter.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. A successful request
                returns the HTTP `204 No Content` status code with no JSON response
                body.

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/billing/subscriptions/{id}/activate")
            .http_method(HttpMethodEnum.POST)
            .template_param(Parameter()
                .key("id")
                .value(id)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("Content-Type")
                .value("application/json"))
            .body_param(Parameter()
                .value(body))
            .body_serializer(APIHelper.json_serialize)
            .auth(Single("Oauth2")),
        ).response(
            ResponseHandler()
            .is_api_response(True)
            .local_error("400",
                "Bad Request. Request is not well-formed, syntactically incorrect, or"
                " violates schema.",
                SubscriptionErrorException)
            .local_error("401",
                "Authentication failed due to missing authorization header, or invali"
                "d authentication credentials.",
                SubscriptionErrorException)
            .local_error("403",
                "Authorization failed due to insufficient permissions.",
                SubscriptionErrorException)
            .local_error("404",
                "The specified resource does not exist.",
                SubscriptionErrorException)
            .local_error("422",
                "The requested action could not be performed, semantically incorrect,"
                " or failed business validation.",
                SubscriptionErrorException)
            .local_error("500",
                "An internal server error has occurred.",
                SubscriptionErrorException)
            .local_error("default", "The error response.", SubscriptionErrorException),
        ).execute()

    def capture_subscription(self,
                             id,
                             pay_pal_request_id=None,
                             body=None):
        """Perform a POST request to /v1/billing/subscriptions/{id}/capture.

        Captures an authorized payment from the subscriber on the subscription.

        Args:
            id (str): The ID of the subscription.
            pay_pal_request_id (str, optional): The server stores keys for 72 hours.
            body (CaptureSubscriptionRequest, optional): The request body parameter.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. A successful request
                returns the HTTP `200 OK` status code and a JSON response body that
                shows subscription details.

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/billing/subscriptions/{id}/capture")
            .http_method(HttpMethodEnum.POST)
            .template_param(Parameter()
                .key("id")
                .value(id)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("Content-Type")
                .value("application/json"))
            .header_param(Parameter()
                .key("PayPal-Request-Id")
                .value(pay_pal_request_id))
            .body_param(Parameter()
                .value(body))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .body_serializer(APIHelper.json_serialize)
            .auth(Single("Oauth2")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(SubscriptionTransactionDetails.from_dictionary)
            .is_api_response(True)
            .local_error("400",
                "Bad Request. Request is not well-formed, syntactically incorrect, or"
                " violates schema.",
                SubscriptionErrorException)
            .local_error("401",
                "Authentication failed due to missing authorization header, or invali"
                "d authentication credentials.",
                SubscriptionErrorException)
            .local_error("403",
                "Authorization failed due to insufficient permissions.",
                SubscriptionErrorException)
            .local_error("404",
                "The specified resource does not exist.",
                SubscriptionErrorException)
            .local_error("422",
                "The requested action could not be performed, semantically incorrect,"
                " or failed business validation.",
                SubscriptionErrorException)
            .local_error("500",
                "An internal server error has occurred.",
                SubscriptionErrorException)
            .local_error("default", "The error response.", SubscriptionErrorException),
        ).execute()

    def list_subscription_transactions(self,
                                       id,
                                       start_time,
                                       end_time):
        """Perform a GET request to
        /v1/billing/subscriptions/{id}/transactions.

        Lists transactions for a subscription.

        Args:
            id (str): The ID of the subscription.
            start_time (str): The start time of the range of transactions to list.
            end_time (str): The end time of the range of transactions to list.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. A successful request
                returns the HTTP `200 OK` status code and a JSON response body that
                shows subscription details.

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/billing/subscriptions/{id}/transactions")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("id")
                .value(id)
                .is_required(True)
                .should_encode(True))
            .query_param(Parameter()
                .key("start_time")
                .value(start_time)
                .is_required(True))
            .query_param(Parameter()
                .key("end_time")
                .value(end_time)
                .is_required(True))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("Oauth2")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(TransactionsList.from_dictionary)
            .is_api_response(True)
            .local_error("400",
                "Bad Request. Request is not well-formed, syntactically incorrect, or"
                " violates schema.",
                SubscriptionErrorException)
            .local_error("401",
                "Authentication failed due to missing authorization header, or invali"
                "d authentication credentials.",
                SubscriptionErrorException)
            .local_error("403",
                "Authorization failed due to insufficient permissions.",
                SubscriptionErrorException)
            .local_error("404",
                "The specified resource does not exist.",
                SubscriptionErrorException)
            .local_error("500",
                "An internal server error has occurred.",
                SubscriptionErrorException)
            .local_error("default", "The error response.", SubscriptionErrorException),
        ).execute()

    def suspend_subscription_1(self,
                               id,
                               body=None):
        """Perform a POST request to /v1/billing/subscriptions/{id}/suspend.

        Suspends the subscription.

        Args:
            id (str): The ID of the subscription.
            body (CancelSubscriptionRequest, optional): The request body parameter.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. A successful request
                returns the HTTP `204 No Content` status code with no JSON response
                body.

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/billing/subscriptions/{id}/suspend")
            .http_method(HttpMethodEnum.POST)
            .template_param(Parameter()
                .key("id")
                .value(id)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("Content-Type")
                .value("application/json"))
            .body_param(Parameter()
                .value(body))
            .body_serializer(APIHelper.json_serialize)
            .auth(Single("Oauth2")),
        ).response(
            ResponseHandler()
            .is_api_response(True)
            .local_error("400",
                "Bad Request. Request is not well-formed, syntactically incorrect, or"
                " violates schema.",
                SubscriptionErrorException)
            .local_error("401",
                "Authentication failed due to missing authorization header, or invali"
                "d authentication credentials.",
                SubscriptionErrorException)
            .local_error("403",
                "Authorization failed due to insufficient permissions.",
                SubscriptionErrorException)
            .local_error("404",
                "The specified resource does not exist.",
                SubscriptionErrorException)
            .local_error("422",
                "The requested action could not be performed, semantically incorrect,"
                " or failed business validation.",
                SubscriptionErrorException)
            .local_error("500",
                "An internal server error has occurred.",
                SubscriptionErrorException)
            .local_error("default", "The error response.", SubscriptionErrorException),
        ).execute()

    def capture_subscription_1(self,
                               id,
                               pay_pal_request_id=None,
                               body=None):
        """Perform a POST request to /v1/billing/subscriptions/{id}/capture.

        Captures an authorized payment from the subscriber on the subscription.

        Args:
            id (str): The ID of the subscription.
            pay_pal_request_id (str, optional): The server stores keys for 72 hours.
            body (CaptureSubscriptionRequest1, optional): The request body parameter.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. A successful request
                returns the HTTP `200 OK` status code and a JSON response body that
                shows subscription details.

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/billing/subscriptions/{id}/capture")
            .http_method(HttpMethodEnum.POST)
            .template_param(Parameter()
                .key("id")
                .value(id)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("Content-Type")
                .value("application/json"))
            .header_param(Parameter()
                .key("PayPal-Request-Id")
                .value(pay_pal_request_id))
            .body_param(Parameter()
                .value(body))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .body_serializer(APIHelper.json_serialize)
            .auth(Single("Oauth2")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(SubscriptionTransactionDetails.from_dictionary)
            .is_api_response(True)
            .local_error("400",
                "Bad Request. Request is not well-formed, syntactically incorrect, or"
                " violates schema.",
                SubscriptionErrorException)
            .local_error("401",
                "Authentication failed due to missing authorization header, or invali"
                "d authentication credentials.",
                SubscriptionErrorException)
            .local_error("403",
                "Authorization failed due to insufficient permissions.",
                SubscriptionErrorException)
            .local_error("404",
                "The specified resource does not exist.",
                SubscriptionErrorException)
            .local_error("422",
                "The requested action could not be performed, semantically incorrect,"
                " or failed business validation.",
                SubscriptionErrorException)
            .local_error("500",
                "An internal server error has occurred.",
                SubscriptionErrorException)
            .local_error("default", "The error response.", SubscriptionErrorException),
        ).execute()
